{% extends 'base.html.twig' %}

{% block title %}Nouvelle permission{% endblock %}

{% block body %}
    <div class="container">
        <div class="row mt-5">
            <div class="col-lg-4 col-md-6" style="background: #f2f2f2;">
                {% include "admin/_menu_dashboard.html.twig" %}
            </div>
            <div class="col-lg-8 col-md-6">
                <h4>Enregister une nouvelle permission</h4>
                {{ include('admin/permission/_form.html.twig') }}
                <a href="{{ path('admin_permission_index') }}" class="btn btn-sm btn-warning mt-1">Retourner à la liste</a>
            </div>
        </div>
    </div>
{% endblock %}
