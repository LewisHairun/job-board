<div class="nav flex-column nav-pills" id="v-pills-tab" role="tablist" aria-orientation="vertical">
    <a class="nav-link active" 
        data-toggle="pill" 
        href="{{ path('candidate_profile') }}" 
        role="tab" 
        aria-controls="v-pills-home" 
        aria-selected="true">
            Profile
    </a>
    <a class="nav-link" 
        data-toggle="pill" 
        href="{{ path('candidate_setting') }}" 
        role="tab" 
        aria-controls="v-pills-profile" 
        aria-selected="false">
        Paramètrage
    </a>
</div>